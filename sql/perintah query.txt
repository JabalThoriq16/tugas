SELECT * FROM 'NAMA TABLE'//UNTUK MENAMPILKAN SEMUA DATA TABLE
SELECT NAMA_COLOUM FROM 'NAMA TABLE' //UNTUK MENAMPILKAN BEBERAPA COLOM DATA TABLE
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM > NILAI //UNTUK MENCARI NILAI YANG LEBIH BESAR PADA COLOUM TERSEBUT
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM = NILAI //UNTUK MENCARI NILAI YANG SAMA PADA COLOUM TERSEBUT
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM < NILAI //UNTUK MENCARI NILAI YANG LEBIH KECIL PADA COLOUM TERSEBUT
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM >= NILAI //UNTUK MENCARI NILAI YANG LEBIH BESAR SAMA DENGAN PADA COLOUM TERSEBUT
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM <= NILAI //UNTUK MENCARI NILAI YANG LEBIH KECIL SAMA DENGAN PADA COLOUM TERSEBUT

{-- MENGURUTKAN DATA TERBESAR KE DATA TERKECIL --}
SELECT * FROM 'NAMA TABLE' ORDER BY NAMA_COLOUM DESC

{-- MENGURUTKAN DATA TERKECIL KE DATA TERBESAR --}
SELECT * FROM 'NAMA TABLE' ORDER BY NAMA_COLOUM ASC

{-- MENCARI NAMA YANG TERKANDUNG --}
SELECT * FROM 'NAMA TABLE' WHERE NAMA_COLOUM LIKE %NAMA_YANG_DICARI%

{-- MENJUMLAHKAN QUANTITY BERDASARKAN ID PENERBIT --}
SELECT id_penerbit, SUM(qty_stock) as total From 'buku' GROUP BY id_penerbit

{-- MENJUMLAHKAN QUANTITY BERDASARKAN ID PENGARANG --}
SELECT id_pengarang, SUM(qty_stock) as total From 'buku' GROUP BY id_pengarang

{-- MENJUMLAHKAN QUANTITY BERDASARKAN ID KATALOG --}
SELECT id_katalog, SUM(qty_stock) as total From 'buku' GROUP BY id_katalog

{-- MENJUMLAHKAN QUANTITY BERDASARKAN TAHUN --}
SELECT tahun, SUM(qty_stock) as total From 'buku' GROUP BY tahun

{-- MENJUMLAHKAN QUANTITY BERDASARKAN TAHUN 2020 diatasnya --}
SELECT tahun, SUM(qty_stock) as total From 'buku' WHERE tahun >= 2020 GROUP BY tahun GROUP BY tahun DESC


{-- MENJUMLAHKAN QUANTITY BERDASARKAN TAHUN DIURUTKAN BERDASARKAN DARI YANG TERBESAR KE TERKECIL --}
SELECT tahun, SUM(qty_stock) as total From 'buku' GROUP BY tahun GROUP BY tahun DESC


{-- MENJUMLAHKAN QUANTITY BERDASARKAN TAHUN DIURUTKAN BERDASARKAN DARI YANG TERKECIL KE TERBESAR --}
SELECT tahun, SUM(qty_stock) as total From 'buku' GROUP BY tahun GROUP BY tahun asc